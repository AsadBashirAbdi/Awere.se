"use client";

import type { ReactNode } from "react";

// Studio-only theme stub for now. This becomes real when we add the blueprint theme.
export function ThemeProvider({ children }: { children: ReactNode }) {
  return <>{children}</>;
}

